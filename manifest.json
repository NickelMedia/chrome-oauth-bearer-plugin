{
  "name": "Kubernetes Chrome Token Plugin",
  "version": "0.8",
  "manifest_version": 3,
  "description": "Adds a given token to the appropriate header for all requests to a given url/domain",
  "icons": { "128": "auth.png" },
  "permissions": ["storage", "webRequest", "*://*/*", "background", "webRequestBlocking", "alarms"],
  "background": {
    "scripts": ["background.js"],
    "persistent":true
  },
  "options_page": "options.html"

}
